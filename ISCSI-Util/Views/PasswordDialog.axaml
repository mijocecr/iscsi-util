<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        x:Class="ISCSI_Util.Views.PasswordDialog"
        Width="360"
        Height="200"
        WindowStartupLocation="CenterOwner"
        Title="Administrator permissions"
        CanResize="False"
        CanMinimize="False"
        CanMaximize="False"
        
        Classes="PasswordDialog">

    <!-- CONTENEDOR PRINCIPAL -->
    <Border Background="{DynamicResource SteamPanel}"
            BorderBrush="{DynamicResource SteamAccent}"
            BorderThickness="2"
            CornerRadius="10"
            Padding="12"
            Margin="6">

        <StackPanel Spacing="14">

           

            <!-- TEXTO -->
            <TextBlock Text="Enter the administrator password to manage iSCSI:"
                       Classes="PasswordDialogMessage"
                       TextWrapping="Wrap"/>

            <!-- PASSWORD BOX -->
            <TextBox x:Name="PwdBox"
                     Classes="PasswordDialogInput"
                     PasswordChar="●"
                     KeyDown="OnPasswordKeyDown"
                     Height="32"/>

            <!-- ERROR MESSAGE -->
            <TextBlock x:Name="ErrorText"
                       Text=""
                       IsVisible="False"
                       Foreground="Red"
                       FontSize="13"
                       Margin="2,0,0,0"/>

            <!-- BOTONES -->
            <StackPanel Orientation="Horizontal"
                        HorizontalAlignment="Center"
                        Spacing="10">

                <Button Content="Cancel"
                        Width="90"
                        HorizontalContentAlignment="Center"
                        Classes="PasswordDialogButton"
                        Click="OnCancel"/>

                <Button Content="OK"
                        Width="90"
                        HorizontalContentAlignment="Center"
                        Classes="PasswordDialogButton"
                        Click="OnAccept"/>

            </StackPanel>

        </StackPanel>

    </Border>
</Window>
